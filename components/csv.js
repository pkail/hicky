import {
	csvParse,
	csv
} from "d3";
import { csvList } from './csvList';

export const fetchData = async() => {

// const csvUrl = "./nodeList.csv";

	// const fullData = await csv("../components/csvList.csv");
	console.log('fullData >>>', fullData)
}

